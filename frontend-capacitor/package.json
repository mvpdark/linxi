{
  "name": "lingxi-capacitor",
  "private": true,
  "version": "1.0.0",
  "description": "灵犀 - Capacitor Android 端打包工程",
  "scripts": {
    "add:android": "cap add android",
    "sync": "cap sync android",
    "copy": "cap copy android",
    "open": "cap open android",
    "build:apk": "npm run sync && cd android && gradlew.bat assembleDebug",
    "build:release": "npm run sync && cd android && gradlew.bat assembleRelease",
    "build:aab": "npm run sync && cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
