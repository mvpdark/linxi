# ===========================================================================
# Docker 构建上下文排除规则（路径相对于【项目根目录】，即构建上下文根）
#
# 注意：本文件位于 backend/ 子目录。Docker 默认只读取上下文根部的 .dockerignore，
# 使用 BuildKit（Docker 20.10+ 默认开启）时，会自动识别 Dockerfile 同目录下的
# 「Dockerfile.dockerignore」。因此二选一：
#   1. 构建前将本文件复制为项目根目录的 .dockerignore；
#   2. 或将本文件重命名/复制为 backend/Dockerfile.dockerignore（推荐，免改动根目录）。
# docker-compose.yml 中的构建已按上下文 = 项目根目录配置。
# ===========================================================================

# -- 版本控制 / IDE ----------------------------------------------------------
.git
.gitignore
.trae/
.vscode/
.idea/

# -- Python 缓存 -------------------------------------------------------------
**/__pycache__/
**/*.pyc
**/*.pyo
**/.pytest_cache/

# -- 运行期数据（通过 volume 挂载提供，不打入镜像） -----------------------------
cache/
assets/uploads/
assets/upload_*
assets/generated_*
assets/mask_*
assets/panorama_*
assets/design_refs/
assets/panorama_test/

# -- SAM2 模型权重（约 150MB，启动时自动下载或 volume 挂载） --------------------
src/checkpoints/*.pt

# -- sam2_src：仅保留 sam2/ 包（Dockerfile 只 COPY 该目录，此处作双保险） -------
src/sam2_src/demo/
src/sam2_src/notebooks/
src/sam2_src/sav_dataset/
src/sam2_src/tools/
src/sam2_src/training/
src/sam2_src/assets/
src/sam2_src/.github/
src/sam2_src/checkpoints/

# -- 敏感配置（含 API Key，严禁打入镜像；运行时以环境变量或挂载方式注入） --------
config.yaml
.env

# -- 部署目录自身 / 文档 / 测试截图等根目录杂物 ---------------------------------
backend/
screenshots/
docs/
design/
/*.png
/*.md
/pyproject.toml
